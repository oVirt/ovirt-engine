ovirt-fence-kdump-listener service configuration directory

Only *.conf are considered.

Files are sorted by name, last wins.

Variables available at:

  @ENGINE_USR@/services/ovirt-fence-kdump-listener/ovirt-fence-kdump-listener.conf
