ovirt-engine-notifier service configuration directory

Only *.conf are considered.

Files are sorted by name, last wins.

Variables available at:

  @ENGINE_USR@/services/ovirt-engine-notifier/ovirt-engine-notifier.conf
