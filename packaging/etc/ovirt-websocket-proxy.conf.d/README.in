ovirt-websocket-proxy service configuration directory

Only *.conf are considered.

Files are sorted by name, last wins.

Variables available at:

  @ENGINE_USR@/services/ovirt-websocket-proxy/ovirt-websocket-proxy.conf
