#
# This file is used to register the provisioal bridge between the new
# directory interfaces and the old LDAP infrastructure. It should be
# removed once the old LDAP infrastructure is retired.
#
org.ovirt.engine.core.authentication.provisional.ProvisionalDirectoryFactory
