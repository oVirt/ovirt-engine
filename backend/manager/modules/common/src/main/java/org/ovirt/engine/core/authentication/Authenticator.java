package org.ovirt.engine.core.authentication;

/**
 * A authenticator is an object used to verify an identity. This interface is empty, the real semantics are in the
 * extensions.
 */
public interface Authenticator {
}
