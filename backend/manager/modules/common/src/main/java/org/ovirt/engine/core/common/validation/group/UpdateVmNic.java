package org.ovirt.engine.core.common.validation.group;

/**
 * Group for VM NICs update (which require special treatment).
 */
public interface UpdateVmNic extends UpdateEntity {
    // Used for validation.
}
