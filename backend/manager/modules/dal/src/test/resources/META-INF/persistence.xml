<persistence xmlns="http://java.sun.com/xml/ns/persistence" version="2.0">
    <persistence-unit name="ovirt">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <properties>
            <property name="javax.persistence.jdbc.driver" value="${engine.db.driver}" />
            <property name="javax.persistence.jdbc.user" value="${engine.db.username}" />
            <property name="javax.persistence.jdbc.password" value="${engine.db.password}" />
            <property name="javax.persistence.jdbc.url" value="${engine.db.url}" />
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect" />
        </properties>
    </persistence-unit>
</persistence>